# Windcode 长期记忆任务

- [x] 固定五类参考实现版本并记录源码路径与许可证。
- [x] 定义记忆类型、作用域、状态、来源、证据、版本及搜索结果公共类型。
- [x] 实现 Markdown 序列化、原子写入、SQLite FTS、检索、生命周期、删除和重建。
- [x] 实现敏感信息拒绝、项目隔离、经验验证证据和有界召回。
- [x] 增加 `MemoryConfig`、SDK 管理 API 与 `MemoryEvent` 契约。
- [x] 在主运行装配召回上下文和显式意图候选；子智能体不获得写接口。
- [x] 增加 `/memory` 状态、候选、搜索、详情、确认、拒绝、删除与重建命令及管理面板。
- [x] 覆盖单元、契约、集成、安全与端到端场景并执行质量门禁。
- [x] 明确运行前按需召回与运行后选择性写入链路，禁止全量记忆上下文注入。
- [x] 增加自然语言主动查询的只读工具、项目隔离、事件观测和 TUI 工具标签。
